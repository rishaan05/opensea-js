"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatTableColumn = void 0;
const markdown_blocks_to_html_1 = require("./markdown-blocks-to-html");
const normalize_line_breaks_1 = require("./normalize-line-breaks");
function formatTableColumn(str) {
    // Normalize line breaks
    let md = (0, normalize_line_breaks_1.normalizeLineBreaks)(str);
    // If comments are on multiple lines convert markdown block tags to HTML and remove new lines.
    if (md.split('\n').length > 1) {
        md = (0, markdown_blocks_to_html_1.markdownBlocksToHtml)(md);
    }
    // Finally return with escaped pipes
    return md.replace(/\|/g, '\\|');
}
exports.formatTableColumn = formatTableColumn;
